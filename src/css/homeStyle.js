import {StyleSheet} from 'react-native';
import * as color  from '../assets/theme/colors';

export default StyleSheet.create({
  wrapper: {},
  textInput: {
      height: 42,
      borderColor: color.grey,
      borderWidth: 1, 
      padding: 10
  }
});
